import Layout from "@/components/layout/Layout";
import { NextPage } from "next";

const RegisterPage: NextPage = () => {
  return <Layout title={"Register page"}>Register</Layout>;
};

export default RegisterPage;
